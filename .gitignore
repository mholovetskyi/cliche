# Build output
/cliche
/cliche.exe
/dist/
*.exe
*.exe~

# Go test artifacts
*.test
*.out
/cliche
cover
cover_*
coverage*
*.coverprofile

# Cliche's own per-project state
.cliche/

# Editor / OS noise
.idea/
.vscode/
.DS_Store

api.txt
*.key
.cliche_key

# Private commercial planning (backend / control-plane / Stripe spec) — never public.
/private/

# Cliche Studio frontend: commit the source, not the toolchain. The BUILD output
# lands in internal/web/static (committed) so `go build` bakes in the UI with no Node.
/studio/node_modules/
/studio/dist/
/studio/.vite/
