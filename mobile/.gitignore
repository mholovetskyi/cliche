node_modules/

# Native projects are generated by `npx cap add ios|android` (iOS needs a Mac).
# Regenerate them rather than committing — they're large and machine-specific.
/ios/
/android/

.DS_Store
