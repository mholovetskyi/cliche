# Normalize line endings: store text as LF in the repo.
* text=auto eol=lf

# Go and shell sources are always LF.
*.go    text eol=lf
*.mod   text eol=lf
*.sum   text eol=lf
*.sh    text eol=lf
*.yml   text eol=lf
*.yaml  text eol=lf
*.md    text eol=lf

# Binaries / artifacts.
*.exe   binary
